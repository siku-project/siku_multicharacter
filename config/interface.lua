InterfaceConfig = {
  --- Interface theme
  ---
  --- Art direction applied to every multicharacter screen (selection,
  --- identity creation and appearance creator). The choice is forwarded
  --- to the NUI on startup, which renders the matching interface.
  ---
  --- The three directions expose the exact same features and the same
  --- flow: only the presentation changes.
  ---
  --- Available:
  ---   'glass'   — Ice Glass panels, frosted surfaces and cold light
  ---   'minimal' — floating islands, ice palette, editorial and airy
  ---   'dark'    — neutral dark mode, SIKU blue kept for accents only
  ---
  --- Default: 'glass'
  theme = 'glass',
}
